@{
    ViewData["Title"] = "Import CSV";
}

<form id="import-csv-form" asp-action="ImportCsv" method="post" enctype="multipart/form-data">
    @Html.AntiForgeryToken()

    <div class="crm-details-actions-strip">
        <a asp-controller="Contacts" asp-action="Index" class="crm-btn-tertiary btn-sm">
            <i class="bi bi-arrow-left" aria-hidden="true"></i> Back to Contacts
        </a>
        <button type="submit" class="crm-btn-primary btn-sm">
            <i class="bi bi-upload" aria-hidden="true"></i> Import Contacts
        </button>
    </div>

    <section class="crm-edit-masthead crm-fade-in">
        <div class="crm-edit-masthead-photo-placeholder" aria-hidden="true"><i class="bi bi-file-earmark-arrow-up"></i></div>
        <div>
            <div class="crm-eyebrow" style="margin-bottom: 6px;">Import</div>
            <h1 class="crm-edit-masthead-title">Import CSV</h1>
        </div>
    </section>

    <div class="crm-page" style="max-width: 720px;">
        <div asp-validation-summary="ModelOnly" class="crm-field-error"></div>

        <div class="crm-card">
            <div class="crm-card-header">
                <h5 class="card-title mb-0"><i class="bi bi-file-earmark-spreadsheet" aria-hidden="true"></i> CSV File</h5>
            </div>
            <div class="crm-card-body">
                <div class="crm-field-group">
                    <label for="file">Select a .csv file to import</label>
                    <input type="file" id="file" name="file" class="crm-input" accept=".csv" required />
                    <div class="crm-field-help">Select a .csv file exported from this app. Duplicates will be skipped.</div>
                    <div class="crm-field-help" style="margin-top: 8px;">
                        Need a template? <a asp-controller="Contacts" asp-action="ExportCsv" class="crm-btn-tertiary btn-sm">Download template</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
