1. **Replace vague variable `data` in `HomeController.Index`**
   - The variable name `data` is vague and doesn't communicate what the data represents.
   - Use `replace_with_git_merge_diff` to rename `data` to `dashboard` in `Rvnx.CRM.Web/Controllers/HomeController.cs`.
2. **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.**
